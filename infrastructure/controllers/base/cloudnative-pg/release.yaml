apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: cloudnative-pg
spec: 
  interval: 30m
  chart:
    spec:
      chart: "cloudnative-pg"
      version: "1.18.2"
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg
        namespace: cloudnative-pg
      interval: 12h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace 
